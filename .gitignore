# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Virtual Environment
venv/
env/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Cache files
*.pyc
code_cache.json
vehicle_cache.json
forum_cache.json

# Database
*.db
cleardrive.db

# Backups (e.g. cleardrive.db.pre-research-logging.bak)
*.bak

# Scraped training data (regenerable via scrape_training_data.py)
training_data/

# ml/ — large/regenerable artifacts. .gitkeep files are kept so the
# directories exist on clone (negation patterns below).
ml/runs/*
!ml/runs/.gitkeep
ml/data/raw/*
!ml/data/raw/.gitkeep
ml/data/processed/*
!ml/data/processed/.gitkeep
ml/data/pairs/*
!ml/data/pairs/.gitkeep

# Secrets
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db
.git-scrub-replacements.txt
