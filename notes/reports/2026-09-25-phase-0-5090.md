# Brief 2 (weekend) Phase 0, section 0.5 plus corpus location: 5090 (2026-09-25)

Read-only scan. Log: `notes/reports/logs/2026-09-25-phase-0-5090.log`, cited below as `L<n>`.
The run started 2026-09-25T23:38Z on host `BLACK_5090` (L1).

This report fills in the section 0.5 that `notes/reports/2026-09-25-phase-0.md` marked BLOCKED, because the A4500 could not reach the 5090 over SSH. It also answers mismatch #8 from that report: where the corpus is.

**Side effect.** `ollama list` launched the Windows Ollama app and server, version 0.30.9 (L22). The app also downloaded a pending self-update to 0.34.4, which has not been installed. I left the app running. Nothing else changed.

## Pre-sync housekeeping (not part of 0.5)
- The working tree on `main` had uncommitted May 2026 pivot docs and agent configs.
- On Austin's instruction they were committed to a separate branch, `local/2026-05-30-pivot-docs` (commit `9267ecb`), and pushed.
- `main` was then clean. Nothing from that branch was merged or cherry-picked onto `brief-2-weekend`.
- `brief-2-weekend` HEAD was `00a8064`, equal to `origin/brief-2-weekend`.

## 0.5 5090 / training environment
- **GPU:** NVIDIA GeForce RTX 5090, 32607 MiB, driver 610.62 (L4).
- **Python:** 3.12.0 is the only interpreter the `py` launcher registers (L9, L52).
  - `python` on PATH resolves through `WindowsApps` (L6).
  - No conda, mamba or uv is installed, and there is no venv in the repo (L53–59).
- **torch:** not installed; `ModuleNotFoundError: No module named 'torch'` (L13).
- **Unsloth:** not installed (L15).
- **transformers, peft, bitsandbytes, trl:** none installed (L17).
- **CUDA toolkit:** `nvcc` is not on PATH and `CUDA_PATH` is unset (L68–70). Only the driver is present; PyTorch wheels ship their own CUDA runtime.
- **WSL2:** distro `Ubuntu` is present, version 2, stopped (L71–73).
- **Ollama:** `C:\Users\conor\AppData\Local\Programs\Ollama\ollama.exe`, version 0.30.9 (L19, L22).
  - `ollama list` shows `gemma4:e4b`, `llama3:8b`, `qwen2.5:7b`, `qwen2.5:3b`, `sqlcoder:7b`/`latest`, `deepseek-r1:70b`, `hf.co/bartowski/DeepSeek-R1-Distill-Llama-70B-GGUF:Q4_K_M` and `deepseek-r1:8b` (L23–32).
  - **No Qwen3 model is on the 5090.**
- **Disk:** C: is the only drive. It is 3.7T with 564G free, 85% used (L40, L60–66). The repo and the HF cache are both on C:.
- **HF cache:** `HF_HOME` and `HF_HUB_CACHE` are unset, so the cache is at the default `C:\Users\conor\.cache\huggingface`, currently 3.1G (L42–44).
- **`OPENROUTER_API_KEY`:** **UNSET** in the process environment, in the repo `.env`, and in the User and Machine environment variables (L45–49).

## Scraped training corpus location
- **Path:** `C:\Users\conor\Documents\ClearDriveProject\ClearDrive\training_data\`, which is inside this repo checkout. It is gitignored via `.gitignore:34` (L75–77, L100).
- **Size:** 5.3G (L79). This matches `ml/CLAUDE.md`'s "5.3 GB corpus".
- **Contents:** 15,005 files: 15,000 `.json` and 5 `.log` (L81–84).
  - `training_data/raw/` has **300** vehicle directories named `<year>_<make>_<model>`, with one `<DTC>.json` per code. 300 × 50 = 15,000, which matches `scrape_training_data.py`'s 300 vehicles × 50 codes (L86–98).
  - The 5 logs are the scrape and backfill logs at the top level (L86–91).
  - The largest file is about 2.9 MB (`2018_ram_2500/P0171.json`) (L102).
- `ml/data/raw/` also exists but holds only `.gitkeep`, per the A4500 Phase 0 report.
- `training_data/raw/` is read-only source evidence (root `CLAUDE.md`).

## Implications for Phases 4–6 on this box
1. **Phase 4.1 cannot be skipped.** There is no torch, Unsloth or training stack, so a full install is needed.
2. **Phase 5 is blocked on `OPENROUTER_API_KEY`.** 2c step 0.4 requires it, and it is not set anywhere on this box. The corpus itself is present here.
3. **Phase 5 must run on this box, or the corpus must be copied.** The corpus exists only here, not on the A4500.
