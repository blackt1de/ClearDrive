# Brief 2 (weekend) Phase 1: Qwen on the A4500, backend serving it (2026-09-25)

Log: `notes/reports/logs/2026-09-25-phase-1.log`, cited below as `L<n>`. The executor host is `ajb1ubuntu`, the A4500 itself. Austin's Phase 1 rulings of 2026-09-25 apply:
- the model is `qwen3:14b-q4_K_M` with no pull;
- `/diagnose` is read as `/interpret`;
- step 1.4.1 is dropped;
- step 1.4.2 is verified with fixture payloads only;
- both `model_version` lines become `qwen3-14b-base`.

The Phase 0 section 0.5 (5090 / training environment) is **deferred to the 5090 session**. It is not blocked. Phases 4 to 6 run in a local Claude Code session on the 5090.

## Model tag serving
- **Model:** `cleardrive-qwen:latest` (`bb3302d8673b`). It is built from `qwen3:14b-q4_K_M` (Qwen3-14B dense, 14.8B params, Q4_K_M) by `~/cleardrive-qwen.Modelfile` (L85–87):
  ```
  FROM qwen3:14b-q4_K_M
  PARAMETER num_ctx 16384
  PARAMETER temperature 0.2
  ```
  `ollama show` confirms `num_ctx 16384` and `temperature 0.2` (L109). `ollama run … READY` replied `READY` (L127).
- **Proof it is serving:** I unloaded the runner, then sent one public `/interpret` request. The model Ollama loaded for it was `cleardrive-qwen:latest` (L276).
- **Decision logged:** `notes/decisions.md` has `[DECIDED] Base model pinned: Qwen3-14B dense — 2026-09-25`. It supersedes `[DECIDED] Pivot to Qwen MoE` and closes `[OPEN] Canonical Qwen SKU`.
- **Thinking mode:** Qwen3 thinks by default. The backend sends no `think` key, and on Ollama 0.24 the reasoning goes to `message.thinking`, not `content`.
  - On M6 the content had all 12 headers, `done_reason stop`, and 1,508 output tokens including thinking, within `num_predict` 2,800. It took 36.5 s (L158–160).
  - No code change was needed. **Risk:** thinking tokens count against `num_predict`, so a longer case could truncate. Phase 2 should count `done_reason=length`.

## Override (b): largest fixture prompt, measured with the model's tokenizer
**Method:**
- Every fixture prompt was captured from the real `/interpret` prompt builder, run in-process. The model call and the database writes were stubbed; retrieval ran for real.
- Each prompt was sent to Ollama with the model's own tokenizer and chat template, and I read back `prompt_eval_count` (`num_predict=1`).
- Both scripts are logged in full at L25–64.

**Results** (L67–81):

| Fixture | Tokens |
|---|---|
| **m6-2014-bank1-lean-misfire-hard (largest)** | **5,822** |
| silverado-2014-p0420-p0300-secondary | 5,122 |
| camry-2010-p0420-marginal | 4,904 |
| accord-2012-p0171-vacuum-leak | 4,619 |
| escape-2013-p1131-mfg-code | 4,583 |
| tacoma-2009-p0171-severe-trim-synthetic | 4,489 |
| f150-2015-p0301-coil | 4,359 |
| civic-2008-p0442-evap | 4,322 |
| bmw-550i-2013-p0171-bank-specific | 4,207 |
| sonata-2011-p0171-no-capability | 4,200 |
| volvo-v70-2004-clean-replay | 2,447 |
| rav4-2018-clean | 1,851 |
| landcruiser-2014-clean-replay | 1,794 |
| audi-a4-2015-clean-replay | 1,305 |

- **No caching effect:** M6 was sent again at the end and gave the same 5,822 (L81), so prefix caching does not change the count.
- **Budget:** 5,822 + `num_predict` 2,800 = **8,622 of 16,384**.
- **Caveats:**
  - The ruling's "~7,900" was the July estimate; this measurement replaces it in `decisions.md`.
  - This is the maximum over the 14 fixtures, not a proven worst case, because retrieval volume varies by vehicle.
  - The count was taken with `think:false`. The backend leaves `think` unset, which may add a few template tokens.

## VRAM after load
| Reading | Used |
|---|---|
| `qwen3:14b-q4_K_M` at 16k context (L135) | 14,071 MiB |
| `cleardrive-qwen` loaded fresh by the backend (L278) | 14,069 MiB |

Total is 20,470 MiB. Both readings are under the 18,500 MiB stop threshold.

## Config lines changed
Commit `cad6465` (pushed).
- `ollama_client.py:26`: `DEFAULT_MODEL = "gemma4:e4b"` → `"cleardrive-qwen"`
- `main.py:1584` (no-codes path): `model_version="gemma4-e4b-base"` → `"qwen3-14b-base"`
- `main.py:2049` (coded path): `model_version="gemma4-e4b-base"` → `"qwen3-14b-base"`
- **Tests written first:** three were added to `test_diagnostics.py`. They failed 3 of 46 before the change and pass 46 of 46 after. They pin the model and assert the research label on both logging paths, with a non-mock snapshot and no database or network access.
- **Code review:** round 1 PASS, no findings. The one advisory: a stale "Gemma 4 E4B" comment at `main.py:2028–2029`, left alone because it is out of scope.
- **`num_ctx` is sent on every call:** `ollama_client.py:86` sends `"num_ctx": 16384` per request. That overrides the Modelfile value, and both are 16384.
- **Not changed:** the Gemma wording in the `ollama_client.py:55` docstring, which is not config.
- **Committed alongside:** `features/brief-2-weekend.md`, whose requirement was set from Austin's answer.

## Health URLs and codes
- **Deploy:** the working folder is the deployed checkout and is already on `brief-2-weekend` at `cad6465`, so there was no branch switch. `sudo systemctl restart cleardrive` brought it back active (L173–176, L218).
- **`http://localhost:8000/health`:** 200 (L223)
- **`https://api.cleardriveapp.com/health`:** 200 (L228)
- Both list `cleardrive-qwen:latest`.
- **cloudflared:** it was running, so step 1.3.6 was not needed.

## Two end-to-end latencies
Both are public `POST https://api.cleardriveapp.com/interpret` calls in scenario mode. Full responses are in the log.

| Fixture | HTTP | Latency | Verdict | Sections | Log |
|---|---|---|---|---|---|
| `accord-2012-p0171-vacuum-leak` | 200 | **33.0 s** | `ok` / SAFE | 12/12 | L232–234 |
| `f150-2015-p0301-coil` | 200 | **27.4 s** | `stop_driving` / STOP | 12/12 | L238–240 |
| `f150-2015-p0301-coil`, after unload (identity check above) | 200 | 38.1 s, includes cold load | | | L269–271 |

On Gemma the mean was 13.6 s (2026-08-21), so Qwen3-14B is about 2 to 2.5 times slower per request. Some of that is thinking time.

## Live-scan path confirmed (1.4.2, per ruling)
- **Rows appeared:** the three fixture requests (`is_mock` true) wrote `scans` rows 14, 15 and 16. `research_scans` stayed at 0, and the journal shows `Skipping research log — mock/demo snapshot` for each (L300–305, L308–318).
- **Live path not exercised with a real request:** the live client path builds its snapshot with a hardcoded `is_mock=False` (`main.py:1314`). No request down that path can be `is_mock` true, and a request with a fixture payload would insert a fake non-mock `research_scans` row, which the ruling forbids. None was sent.
- **What covers it instead:**
  - the code path `main.py:1314` → the `is_mock` gate at `main.py:2045` → `log_research_scan(model_version="qwen3-14b-base", …)`;
  - the two new unit tests, which drive `log_research_scan` with a non-mock snapshot on both paths.
- **First real row:** that will be Austin's first scan from the phone.

## pytest and test_api.py (ruling d)
- **pytest:** 9.1.1 installed into `.venv` (L6).
- **`pytest -q test_api.py`: "no tests ran"** (L11). The file has no test functions (L15). It is a 13-line script that fetches Jeep models from fueleconomy.gov and prints them, so there is nothing for pytest to collect.
- **Real suite:** `pytest -q test_diagnostics.py` gave 43 passed before the change (L21) and 46 passed after.

## Bugs observed but not fixed
1. **NHTSA complaints lookups return HTTP 400.** It happens for 2015 F-150 (L202, production journal), 2014 Land Cruiser (both `+` and `%20` spacing), and `A4 quattro`; plain `A4` returns 200.
   - The fixture prompts therefore carry no NHTSA complaints for these vehicles.
   - This bears on the replay pilot's criterion 2 (retrieval source set) and on the Phase 2 eval, which reuses the knowledge.py complaints client (2a step 3).
   - The code files are frozen this weekend, so it is not fixed. It needs a ruling before Phase 2.
2. **Stale "Gemma 4 E4B" comments** at `main.py:2028–2029` and `ollama_client.py:55`.

## Anything blocked
Nothing is blocked in Phase 1.
- The 5090 environment check (Phase 0.5) is deferred to the 5090 session.
- The Phase 0 report section 0.5 has been relabelled "deferred to 5090 session".
