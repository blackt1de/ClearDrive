# Brief 2 (weekend) Phase 2: eval set, freeze, baseline run (2026-09-26) — INTERIM, BLOCKED

Log: `notes/reports/logs/2026-09-25-phase-2.log`. This follows Brief 2a (`docs/briefs/weekend/brief-2a-eval-set-freeze.md`) as amended by Austin's rulings; every ruling is in `notes/decisions.md`.

**Status.** Steps 0–11 and 13 are done for the rule-based arm. The **base-Qwen run stopped at the truncation gate**: 21 of 132 calls (15.9%) were truncated, against a 5% limit. Per the 2026-09-25 ruling, that run is *not scored*. The thinking-off base run was not started. Production is serving thinking-on (`/health`: `serving {model: cleardrive-qwen, think: default}`).

## 1. Step 0 findings (pre-existing eval state)
No `eval/` directory, eval set, manifest or freeze tag existed. `ml/src/eval/` was an empty package.

## Pre-freeze fixes (ruled, applied to every condition before any run)
- **NHTSA model-name resolution** (`knowledge.py`).
  - NHTSA answers 400 with an empty result when it files the vehicle under another name.
  - On a 400 the code now resolves against NHTSA's own model list per endpoint: exact match, then trim-stripped, then body-style variants.
  - Code review caught a prefix match that would have attached COROLLA CROSS complaints to a COROLLA query.
  - Regression set, complaints before → after: F-150 0 → 10, Land Cruiser 0 → 2, A4 quattro 0 → 10.
- **Output budget and finish reason.** `num_predict` goes from 2,800 to 4,096, thinking stays at the model default, and `/interpret` gains an additive `finish_reason`.
- **`eval_case` transport and `CLEARDRIVE_THINK` switch.** `/health` reports `serving`. Eval cases are forced mock (never written to `research_scans`), make no CarsXE call, and are fully validated.
- **Backend model timeout** goes from 180 s to 300 s.

## 2. Taxonomy
There are 15 labels (`eval/taxonomy.json`), P codes only.
- **Grounded in `diagnostics.py` rules (8):** vacuum_intake_leak, fuel_delivery, maf_sensor, ignition_component, fuel_injector, catalytic_converter, oxygen_sensor, evap_system.
- **SAE family fallbacks (7):** valve_timing_air_aux (P00), air_fuel_metering_other (P01), ignition_system_sensor (P03), emissions_control_other (P04), idle_speed_auxiliary (P05), control_module (P06), transmission (P07/P08).
- **Merges, both logged in the file:**
  - fuel_injector merges the injector part of the misfire rule with dtc_family P02.
  - transmission merges P07 with P08.

## 3. Composition
See `eval/composition.md`. **102 cases:**
- 62 reconstructed from NHTSA complaints with a confirmed repair;
- 40 synthetic, each checked against the rule engine at build time;
- 39.2% synthetic, every label at 6 or more, 14 makes;
- 28 pre-CAN and 74 CAN. **OBDonUDS has 0 cases (ruled gap).**

**Codeless (H4): 30 profiles**, with 119 NHTSA-recall documented issues across 14 makes.

How the reconstructed cases were selected (`eval/sources/adjudication.json`):
- about 1.1M complaints swept;
- 12,007 state a P code;
- 780 remained after the shortlist;
- **251 read in full, 62 accepted**, each with a quoted repair sentence;
- `verified_by` is pending Austin's spot-check, and corrections become `eval-v1.1`.

Reconstructed payloads carry the vehicle and the stated DTCs only, with every measurement null (ruling). `known_issues.json` was not used because it is unsourced.

## 4. Freeze
- **Tag:** `eval-v1-frozen` at `aa308a1`.
- **Manifest:** `eval/MANIFEST.json`, holding sha256 hashes for `eval_set.json`, `codeless_set.json`, `taxonomy.json` and `schema.json`.
- **Validator:** `eval_validate.py` reported 0 errors.
- **Reproducibility test:** a test rebuilds the set from `eval/sources/*` and checks the hashes.

## 5. Serving state at baseline
| Item | Value |
|---|---|
| Server | Ollama 0.24.0 |
| Model | `cleardrive-qwen` (qwen3:14b-q4_K_M) |
| `num_ctx` | 16384 |
| `num_predict` | 4096 |
| Thinking | default (on) |
| Backend commit | `bed0615`, clean tree |
| Recorded in | `eval/runs/base-qwen3-14b-2026-09-26/run_meta.json` |

## 6. Baseline scores
**Rule-based arm** (`eval/runs/rulebased-2026-09-26/scores.md`):
- **H1 macro-F1 0.740**, top-1 accuracy 0.755.
- H4 0.0: code lookup cannot name recalls.
- H4 nulls: system-level **0.0**, part-level **0.333**, both pre-registered.
- This arm is strong partly by construction. Synthetic and SAE-family cases carry codes that map directly to their labels. Its weak labels are vacuum_intake_leak (F1 0, since no code names a leak) and control_module (0.50).

**Base-Qwen, thinking on — NOT SCORED (truncation gate FAIL).**
- 132 calls, 0 failed.
- **21 truncated (`finish_reason: length`), all of them codeless profiles:** 21 of 30. None of the 102 coded cases truncated.
- A truncated response has empty SUMMARY, SERVICE and KNOWN ISSUES sections (for example CL-004). Thinking used the whole 4,096-token budget before the model wrote any output.
- Latency (descriptive only): coded cases mean 36.5 s (max 63.4); codeless mean 74.1 s (max 95.4); wall time 99 minutes.
- Finding from the dry run, recorded and not fixed (ruling): on code-only reconstructed cases, base Qwen writes "the evidence does not narrow this down to a specific cause" in LIKELY CAUSES.

## 7. Unmapped predictions and parser failures
Not computed for the base run, which is unscored. The rule-based arm had 0.

## 8. Bugs observed but not fixed
- **No-codes truncation.** Thinking-on plus `num_predict` 4096 truncates 70% of no-codes responses. **This also hits production.** A clean car scanned from the phone takes the same no-codes path, so the replay pilot's real arm tomorrow (three clean cars) would mostly return empty text.
- **Unmapped DTC groups.** P28xx–P29xx are not mapped in the rule-based arm (for example P291F in EV-033). EV-033's truth label is fuel_injector, so its score is unaffected.
- **Stale comments.** Gemma wording remains at `main.py:2028` and in the `ollama_client.py` docstring.

## 9. Blocked
**BLOCKED: 2a step 12 (base run), truncation gate.** A ruling is needed on the output budget or thinking mode for the no-codes path. That ruling also applies to Phase 6 and to the phone scans. Options, all of which would apply to every condition:
- **(a) Raise `num_predict` to 8192.** It fits: the largest measured prompt, M6 at 5,822 tokens, plus 8,192 is 14,014 of 16,384. Codeless prompts are about 1,300–2,500 tokens. Worst-case generation is about 150 s, under the 300 s timeout. Then rerun base thinking-on (about 100 min). This is a fixed-model condition, not a touch.
- **(b)** Keep 4,096 and serve with thinking off in production. The thinking-on arm still has to be measured, and would still truncate.
- **(c)** Report as is: H4 for base thinking-on would be scored as misses on truncated profiles.

The base run's raw responses and `run_meta.json` are committed as a record. The base run is not scored, per the gate.
