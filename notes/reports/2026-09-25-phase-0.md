# Brief 2 (weekend) Phase 0: deep state scan (2026-09-25)

Read-only scan. Log: `notes/reports/logs/2026-09-25-phase-0.log`, cited below as `L<n>`.
The run started 2026-09-25T23:19Z (L1).

**Executor host.** This scan ran on `ajb1ubuntu`, which is the A4500 itself (L745, L761). It did not run on the Windows/5090 box the brief assumes. Section 0.2 was therefore run locally rather than over SSH, and section 0.5 could not be run (see 0.5).

**Overrides given at hand-off, which apply to later phases:**
- (a) Companion briefs live at `docs/briefs/weekend/`, not `notes/briefs/`.
- (b) The Phase 1 Modelfile uses `num_ctx 16384`, and Phase 1 measures and records the token count of the largest fixture prompt using the model's own tokenizer.
- (c) `[OPEN] Scraped content in the training corpus` is closed at the start of Phase 5 as "facts only, Opus-written targets, no verbatim prose".

**Side effects of Phase 0.** The one public scenario call required by 0.3 wrote `scans` row id 13. It is a fixture, so the `is_mock` gate kept it out of `research_scans`. That call also loaded `gemma4:e4b` into VRAM. The `cloudflared` tunnel token appeared in `systemctl status` / `ps` output. It was redacted from the log before commit (L819, L874), and later commands filter it.

## 0.1 Repo
- **Branch and status:**
  - On branch `brief-2-weekend`. The only untracked path is `notes/reports/` (L2–11); `.venv/` is now ignored (commit `ec173ae`).
  - HEAD is `ec173ae`, on top of `16718ed` (the briefs) and `9cfe192` (PR #18) (L41–42).
  - Local branches: `brief-2-weekend`, `fix/post-council-cleanup` (stale, from May), `main` (L14–37).
- **Tags:** only `V1.1_Alpha_Release` and `V2.0_Beta_Release`. There is no eval tag (L69–70).
- **Remote:** `git@github.com:blackt1de/ClearDrive.git` (L74). Push from this box works; `brief-2-weekend` is pushed.
- **PRs:** #1–#18, all MERGED, none open (L79–96).
- **Brief directories:** `notes/ai-collab/` and `notes/briefs/` do not exist. The briefs are in `docs/briefs/` and `docs/briefs/weekend/` (L292–300).
- **Entry points:** there is one FastAPI app, `main.py:34`.
  - Diagnosis is `POST /interpret` (`main.py:1252`). **There is no `/diagnose` route** (L303–328).
  - Scenario mode takes the body `{"scenario": "<name>"}` (L962).
- **Model selection** (L331–400):
  - `ollama_client.py:26` sets `DEFAULT_MODEL = "gemma4:e4b"`. This is the only line that selects the model.
  - `ollama_client.py:86` sends `"num_ctx": 16384` explicitly on every call.
  - `ollama_client.py:14` reads the host from env `OLLAMA_HOST` (defaults to `100.100.254.15`), and `.env:4` sets `OLLAMA_HOST=localhost`.
  - Research-log labels are hardcoded as `model_version="gemma4-e4b-base"` at `main.py:1584` and `main.py:2049`. They do not select the model, but they will mislabel `research_scans` rows once Qwen serves.
  - No Modelfile or SGLang config exists in the repo.
- **Scan storage:**
  - `research_scans` is a **SQLite table** in `cleardrive.db`, not a directory. **No `research_scans/` directory exists** (L456–459).
  - `cleardrive.db` has the tables `scans`, `followups`, `feedback`, `research_scans` (L570).
  - Row counts: `scans` 12 (L571; 13 after the 0.3 call), `research_scans` 0, `feedback` 0, `followups` 0 (L572).
  - Mock rows are gated out of `research_scans` on `snapshot.is_mock` (`main.py:1580`, `main.py:2045`; L597–599). The `research_scans` table itself has no `is_mock` column (L577).
- **Fixtures:**
  - There is no `fixtures/` directory. `fixtures.py` exports `SCENARIOS` with **14** entries (L524–537).
  - The 14 are: 10 coded, the RAV4 clean case, and 3 clean replay-pilot cars.
- **Eval:** no `eval/` directory and no eval tag (L467–468).
- **Training data:**
  - Every `ml/data/{raw,processed,pairs,splits}` directory holds only a `.gitkeep` (L614).
  - `ml/data/sae_j2012.json` has 418 entries (L735).
  - **No `.jsonl` exists anywhere in the repo** (L481–482).
  - The scraped corpus is **absent on this box**:
    - `scrape_training_data.py:80` writes to `./training_data/`, and `.gitignore:34` ignores it (L648–654).
    - `ml/CLAUDE.md:9` describes it as a 5.3 GB corpus (L655).
    - No `training_data/` directory was found within depth 4 of `/` (L656–657).
- **`known_issues.json`:** top key `vehicles`, a list of **15** entries with keys `codes, general_issues, make, model, year_end, year_start` (L1030).
- **DTC tiers:** over the 418 J2012 codes, `resolve()` gives **`standardized_unverified` 405, `structural_only` 13, `oem_verified` 0** (L736). No code path emits `TIER_OEM`; it is only defined at `dtc_definitions.py:69` (L741).
- **Tests:**
  - `pytest` is not installed in `.venv` or the system Python, so `pytest -q` fails (L581, L627).
  - `python test_diagnostics.py` passes: **43 passed, 0 failed** (L636).

## 0.2 A4500 (run locally on this host)
- **Tailscale:** this host is `ajb1ubuntu` at 100.100.254.15 (L745). `black-5090` is at 100.114.44.47, Windows, "active; direct" (L749).
- **Host and GPU:** uptime 14 min, meaning a reboot at about 23:06Z. RTX A4500, 20470 MiB total, 0 MiB used before any request (L761).
- **Ollama:** version 0.24.0.
  - `ollama list` shows `qwen3:14b-q4_K_M` (9.3 GB, 6 weeks old) and `gemma4:e4b` (9.6 GB) (L767–768).
  - `qwen3:14b-q4_K_M`: arch qwen3, 14.8B, Q4_K_M, context length 40960 (L1034–1038). Digest `bdbd181c33f2…` (L897).
  - `ollama ps` showed nothing loaded before the 0.3 call.
- **Services:** `cleardrive`, `ollama` and `cloudflared` are all active/running. Unrelated services on the same box: `cloudflared-flexagg` and `flexagg-api` (L770–776).
- **Ollama overrides:** `OLLAMA_HOST=0.0.0.0:11434`, `FLASH_ATTENTION=1`, `KV_CACHE_TYPE=f16`, `NUM_PARALLEL=2`, `KEEP_ALIVE=24h` (L865–869).
- **Processes:** uvicorn `main:app` on `0.0.0.0:8000`, PID 1387. No SGLang process (L876).
- **Deployed checkout:**
  - `cleardrive.service` has `WorkingDirectory=/home/abrennan/cleardrive` (L835). **That is this working tree.** Checking out a branch here changes what production runs on its next restart.
  - The running process started at 23:06:43 (L888), before this session's pull (23:17:59) and branch switch (23:19:21) (L891–892).
  - So it runs `6702d77` code. Code is identical to `9cfe192`; only `notes/decisions.md` changed.
- **Health:** `localhost:11434/api/tags` lists both models (L897). `localhost:8000/health` returned HTTP 200 (L900–901).
- **Tunnel:**
  - There is no `~/.cloudflared/config.yml`. The ClearDrive tunnel runs from a token in the unit file.
  - `cloudflared tunnel list` shows only the `flexagg` tunnel (L909).
  - The journal shows reconnect errors at 23:06–23:07 after boot, then a connection registered on ewr16 (L918–933). Its version, 2026.5.0, is flagged as outdated.
- **Disk:** `/` is 98G with 36G free, 62% used; `/home` is on the same volume (L914–915).

## 0.3 Public endpoint
- **DNS:** `api.cleardriveapp.com` resolves to Cloudflare, 172.67.223.71 and 104.21.91.149 (L941–948).
- **Health:** `GET /health` returned 200, reporting the models `qwen3:14b-q4_K_M` and `gemma4:e4b` (L953–958).
- **Scenario call:** `POST /interpret` with `{"scenario":"f150-2015-p0301-coil"}` (L960–964).
  - HTTP 200 in **30.1 s**. This includes a cold model load; `ollama ps` afterwards shows `gemma4:e4b` loaded (L964, L969).
  - Result: `safety.verdict = stop_driving`, `safety_level = STOP`, `scan_id = 13`.
  - `data_sources`: NHTSA Recalls Database and ClearDrive Known Issues KB.
  - The full response is at L963.
- **After the call:** `ollama ps` shows `gemma4:e4b`, 11 GB, 100% GPU, context 16384. VRAM used is 10487 MiB (L969–971).

## 0.4 iOS
- **Project:** `ios/ClearDrive/ClearDrive.xcodeproj` (L977).
- **Base URL:** `APIClient.swift:30` defaults to `https://api.cleardriveapp.com`, but a `UserDefaults` `serverURL` overrides it (L986). An install that previously saved a Tailscale/dev URL would keep using it.
- **Build:** not attempted; there is no Xcode here. The July iOS changes (`a3c6742`) have never been compiled.

## 0.5 5090 / training environment
**DEFERRED TO 5090 SESSION** (Austin's ruling, 2026-09-25). Phases 4, 5 and 6 run in a local Claude Code session on the 5090, and that session re-runs 0.5 there; this host never needs to reach the 5090. When I first wrote this report the section was marked BLOCKED: `black-5090` is up on Tailscale (L749), but SSH to it times out on port 22, by hostname and by IP (L1005–1006).

- **This box, the A4500:** `.venv` has no `torch`, `unsloth`, `transformers` or `peft` (L1020–1021).
- **`OPENROUTER_API_KEY`:** **UNSET** in both the environment and `.env` (L1010–1011). The `.env` key names present are `CARSXE_API_KEY`, `AUTODEV_API_KEY` and `OLLAMA_HOST`.

The following are all **UNKNOWN** on the 5090:
- the `nvidia-smi` output
- torch and CUDA
- Unsloth
- Ollama
- disk space
- `HF_HOME`

## 0.6 Delta against this brief
- **Backend on the A4500 is running and reachable at api.cleardriveapp.com: TRUE.** Local and public health both return 200, and a public `/interpret` call returned 200 (L901, L958, L964).
- **Ollama on the A4500 currently serves Gemma: TRUE.** The tag is `gemma4:e4b` (`ollama_client.py:26`; `ollama ps` at L969).
- **A Qwen model is already present on the A4500: TRUE.** The tag is `qwen3:14b-q4_K_M` (14.8B, Q4_K_M), pulled about six weeks ago (L767, L1034–1038). The brief's `qwen3:14b` pull may be redundant; I have not verified whether it is the same digest.
- **An eval set exists; a freeze tag exists: FALSE / FALSE** (L467–470).
- **Training JSONL exists: FALSE.** None exist in the repo, and the raw scraped corpus is not on this box (L481, L614, L656).
- **Unsloth imports cleanly on the 5090 with CUDA: UNKNOWN.** The 5090 could not be reached (L1005–1006).
- **`research_scans` has any real (non-fixture, non-synthetic) captures: FALSE.** The table has 0 rows (L572).
- **The backend saves live scans to disk, and where: TRUE, but not as files.**
  - Every `/interpret` writes a `scans` row, and non-mock scans also write a `research_scans` row. Both tables are in `cleardrive.db` (`main.py:1578`, `main.py:2020–2045`; L414–418, L597–599).
  - The row stores the prompt, response, vehicle, codes and data sources (schema at L588).
  - There are no JSON files and no `research_scans/live/` directory.

**Other mismatches between the brief and reality. Each one affects a later phase and needs Austin's ruling before that phase:**
1. **Executor host.**
   - Phases 0–3, 5 and 7 are specified for the Windows box. The executor is actually running on the A4500, and the 5090 has no reachable SSH.
   - Phases 4 and 6 need either Claude Code running on the 5090, or OpenSSH enabled there.
2. **Endpoint name.** The brief says `/diagnose`; the real endpoint is `/interpret`. This affects Phase 1.3.5, Phase 1.4, and 2a steps 2 and 10.
3. **`research_scans` is a database table, not a directory.**
   - Phase 1.4 as written would create a new file sink at `research_scans/live/`, alongside the table rather than replacing it.
   - Phase 3.1 (rsync `research_scans/live/`) and Appendix A's `README-<car>.md` depend on that sink.
   - The pre-registered replay protocol (PR #18) says real captures "land in `scans`/`research_scans` server-side", meaning the table.
4. **The Phase 1.1 pin conflicts with the decision log.**
   - `[DECIDED] Pivot to Qwen MoE — 2026-07-27` fixes the family as MoE.
   - `[OPEN] Canonical Qwen SKU` says "Do not pin by estimate".
   - The original briefs-v2 Gate 3 blocks QLoRA until the 5090 is back **and** Unsloth sm_120 is verified.
   - Pinning Qwen3-14B dense supersedes the MoE decision. That is decision-bearing, so it needs an explicit Austin ack before Phase 1.
5. **`model_version` labels.** `main.py:1584` and `main.py:2049` hardcode `gemma4-e4b-base`. Switching only `ollama_client.py:26` would log Qwen output under a Gemma label in `research_scans`. Phase 1.3 would also need to change these lines.
6. **Branch naming.** 2a step 8 says to commit on branch `brief-2a-eval-freeze`; the master brief says to use one branch, `brief-2-weekend`.
7. **Phase 6 context size.** The Phase 6 Modelfile still says `num_ctx 8192`, while override (b) sets 16384 for Phase 1 only. `ollama_client.py:86` sends 16384 per request regardless.
8. **Phase 5 is blocked here** until someone places the scraped corpus (`training_data/`, 5.3 GB) and `OPENROUTER_API_KEY` where the executor can reach them.
9. **2a step 3** needs "top 12 US nameplates by BTS fleet share". No such source is in the repo, so a sourced list is needed before Phase 2.
10. **`pytest` is missing from `.venv`**, so `test_api.py` cannot run. `test_diagnostics.py` runs standalone.
