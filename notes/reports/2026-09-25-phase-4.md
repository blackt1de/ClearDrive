# Brief 2 (weekend) Phase 4: 5090 training environment (2026-09-25)

Host: `BLACK_5090`. Log: `notes/reports/logs/2026-09-25-phase-4.log`, cited below as `L<n>`.
Pinned model, from `notes/decisions.md` 2026-09-25 (commit `cad6465`): HF `Qwen/Qwen3-14B`, with `unsloth/Qwen3-14B` as the fallback.

## Summary
| Item | Value |
|---|---|
| Unsloth | 2026.9.11; unsloth_zoo 2026.9.7 (L21–39) |
| Stack | torch 2.12.1+cu130, transformers 5.5.0, trl 0.24.0, peft 0.21.0, bitsandbytes 0.50.2, triton-windows 3.8.0.post28, xformers 0.0.35 (L21–47) |
| Blackwell check | `ml/scripts/blackwell_check.py` PASS: sm_120, sdpa settable on `Qwen/Qwen3-14B` (L52–62) |
| Smoke load | `Qwen/Qwen3-14B`, resolved by Unsloth to `unsloth/qwen3-14b-unsloth-bnb-4bit`; dtype bfloat16 (L101–102) |
| VRAM at load | **11.23 GB** `torch.cuda.memory_allocated()` at max_seq_length 4096, 4-bit (L103) |
| Disk free | C: 545G free of 3.7T after the download (L106). Model cache is 11G (L107); `.venv` is 3.8G (L108) |
| Scripts | `ml/smoke_load.py` (run), `ml/train_qlora.py` (not run), `ml/merge_and_export.py` (not run) |

## 4.1 Install
- **Doc followed:** https://unsloth.ai/docs/get-started/install/windows-installation, which says: conda env with Python 3.12, then torch from the CUDA wheel index, then `pip install unsloth` (L2).
- **Deviation:** conda is not installed, so I used a stdlib venv at the repo-root `.venv/` instead. `.venv/` is gitignored at `.gitignore:10`.
- **Exact commands:** at L3, L4, L6, L12, L42 and L48.
  1. `py -3.12 -m venv .venv`
  2. `pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu130`
  3. `pip install unsloth`
  4. `pip install --force-reinstall --no-deps torch==2.12.1 torchvision==0.27.1 --index-url https://download.pytorch.org/whl/cu130`
  5. `pip uninstall -y torchaudio`
- **Why steps 4 and 5 were needed:**
  - `pip install unsloth` downgraded torch to the **CPU** build `2.12.1+cpu`, so CUDA was unavailable (L41).
  - I reinstalled the same torch/torchvision versions from the cu130 index. CUDA came back, with capability (12, 0) (L47).
  - The leftover torchaudio 2.11 did not match torch 2.12.1 and isn't used, so I removed it.
  - `pip check` reports no broken requirements (L51).
- **Decision-log note:** the 2026-05-10 entry says "Unsloth Studio install path, not pypi `unsloth`". That was about Gemma 4 kernel patches. Brief 2 Phase 4.1 specifies pip, so I followed the brief.

## 4.2 Smoke
- `ml/smoke_load.py` loads the model with `FastLanguageModel.from_pretrained(model_name, max_seq_length=4096, load_in_4bit=True)` and prints the model name, dtype and allocated VRAM.
- The run passed at 23:47–23:49Z (L72–104).
- Unsloth reported: "Fast Qwen3 patching … RTX 5090 … Platform: Windows … Bfloat16 = TRUE" (L95–97).

## 4.3 `ml/train_qlora.py` (written, not run)
- **As specified:**
  - Unsloth QLoRA on `Qwen/Qwen3-14B` in 4-bit.
  - r=16, alpha=32, dropout 0.05, targeting q/k/v/o/gate/up/down_proj.
  - lr 2e-4, cosine, warmup_ratio 0.03, 2 epochs, batch 4, grad accum 4, max_seq_length 4096, bf16, seed 42.
  - Trains on `data/train/train.jsonl` and evaluates on `data/train/val.jsonl` every 100 steps.
  - Saves the adapter to `ml/out/cleardrive-qwen-lora/` and appends every trainer log event to `ml/out/train_log.jsonl`.
- **Chat template:** the model's own, via `tokenizer.apply_chat_template()`.
- **Prompt masking:** Unsloth `train_on_responses_only` with `<|im_start|>user\n` / `<|im_start|>assistant\n`.
  - I checked this against the real Qwen3 tokenizer: one of each marker per pair (L81–82).
- **Additions beyond the brief:**
  - `--pilot` flag: 5% of train, `max_steps=200`, saved to a `-pilot` adapter dir. The root `CLAUDE.md` requires this pilot before any full run.
  - `--report-to` flag, default `none`. `ml/CLAUDE.md` says to log to W&B, but no W&B key or project is set up for Qwen.
  - A length guard: the script counts tokens per pair and refuses to train if any pair exceeds `--max-seq-length`, because truncation would cut the assistant target.
- **`py_compile`:** OK (L75).

## 4.4 `ml/merge_and_export.py` (written, not run)
1. Loads `ml/out/cleardrive-qwen-lora/` (4-bit, as trained).
2. Runs `save_pretrained_merged(..., "merged_16bit")` with bf16 dtype, writing to `ml/out/cleardrive-qwen-merged/`.
3. Runs `save_pretrained_gguf(..., quantization_method="q4_k_m")` and moves the Q4_K_M file to `ml/out/cleardrive-qwen-Q4_K_M.gguf`.

Both Unsloth APIs exist with these parameter names in the installed version (`unsloth/save.py:2232`, `:3774`). `py_compile` is OK.

Also added: `.gitignore` now ignores `ml/out/*` except `train_log.jsonl` and `train_stdout.log`, so adapters, merged weights and the GGUF can't be committed.

## Flags for Austin before Phase 6 (not blocking Phase 4)
1. **max_seq_length 4096 is probably too short.**
   - Phase 1 measured the backend's largest fixture prompt at **5,822 tokens** (`notes/decisions.md` 2026-09-25). One output was 1,508 tokens.
   - 2c step 7 requires the pairs to use the backend's exact prompt layout, so many pairs will be over 4096.
   - The script will refuse to train rather than truncate targets.
   - Likely fix: `--max-seq-length 8192`. That may need batch 2 × accum 8 (same effective batch of 16) to fit in 32 GB. The merge script's `MAX_SEQ_LENGTH` should match.
2. **Qwen3's template inserts an empty `<think>\n\n</think>\n\n` before every assistant target** (L81).
   - The fine-tune will learn to answer without reasoning.
   - Base Qwen on Ollama currently thinks by default (decisions.md 2026-09-25).
   - That is an H1 comparability question: should base be served with thinking off, or should ft be served as-is? It's a research call for Austin.
3. **Unsloth swaps in the pre-quantized mirror.** It auto-maps `Qwen/Qwen3-14B` to `unsloth/qwen3-14b-unsloth-bnb-4bit` (L101): the same weights, Unsloth's dynamic 4-bit quant.
   - To force the exact repo, pass `use_exact_model_name=True`, which bitsandbytes then quantizes on load.
   - The current scripts accept the default.
4. **GGUF export is unverified on Windows.** There is no cmake, MSVC or gcc on this box (L84–87). The export relies on Unsloth's prebuilt llama.cpp binaries for Windows. If that fails in Phase 6.3, the fallback is WSL2 (Ubuntu is installed).
5. **lora_dropout.** Unsloth's fastest LoRA path assumes `lora_dropout=0`. I kept 0.05 as the brief specifies.

## Phase 5 readiness
- **Tag `eval-v1-frozen`:** not yet present. It comes from the A4500's Phase 2.
- **`OPENROUTER_API_KEY`:** **UNSET** on this box (`notes/reports/2026-09-25-phase-0-5090.md`). 2c step 0.4 will STOP without it.
- **Corpus:** present at `training_data/raw/`: 300 vehicles, 15,000 JSON files.
