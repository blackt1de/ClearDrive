# ClearDrive — required environment variables
# Copy to .env (gitignored) and fill in real values for local dev.
# Production values are set in the PaaS dashboard.

# CarsXE — VIN decode + trim/spec lookup (vehicle_data.py)
CARSXE_API_KEY=

# Auto.dev — high-quality vehicle imagery (vehicle_data.py)
AUTODEV_API_KEY=
